{% extends 'base.html.twig' %}

{% block title %}Commandeitem{% endblock %}

{% block body %}
    <h1>Commandeitem</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idcommandeitem</th>
                <td>{{ commandeitem.idcommandeitem }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ commandeitem.quantity }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_commandeitem_index') }}">back to list</a>

    <a href="{{ path('app_commandeitem_edit', {'idcommandeitem': commandeitem.idcommandeitem}) }}">edit</a>

    {{ include('commandeitem/_delete_form.html.twig') }}
{% endblock %}
