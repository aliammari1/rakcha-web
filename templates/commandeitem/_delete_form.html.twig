<form method="post" action="{{ path('app_commandeitem_delete', {'idcommandeitem': commandeitem.idcommandeitem}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ commandeitem.idcommandeitem) }}">
    <button class="btn">Delete</button>
</form>
