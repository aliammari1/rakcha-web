{% extends 'base.html.twig' %}

{% block title %}CategorieEvenement index{% endblock %}

{% block body %}
    <h1>CategorieEvenement index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>NomCategorie</th>
                <th>Description</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for categorie_evenement in categorie_evenements %}
            <tr>
                <td>{{ categorie_evenement.id }}</td>
                <td>{{ categorie_evenement.nomCategorie }}</td>
                <td>{{ categorie_evenement.description }}</td>
                <td>
                    <a href="{{ path('app_categorie_evenement_show', {'id': categorie_evenement.id}) }}">show</a>
                    <a href="{{ path('app_categorie_evenement_edit', {'id': categorie_evenement.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_categorie_evenement_new') }}">Create new</a>
{% endblock %}
