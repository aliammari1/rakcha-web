{% extends 'base.html.twig' %}

{% block title %}CategorieEvenement{% endblock %}

{% block body %}
    <h1>CategorieEvenement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie_evenement.id }}</td>
            </tr>
            <tr>
                <th>NomCategorie</th>
                <td>{{ categorie_evenement.nomCategorie }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ categorie_evenement.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_categorie_evenement_index') }}">back to list</a>

    <a href="{{ path('app_categorie_evenement_edit', {'id': categorie_evenement.id}) }}">edit</a>

    {{ include('categorie_evenement/_delete_form.html.twig') }}
{% endblock %}
