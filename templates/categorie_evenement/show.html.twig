{% extends 'baseBack.html.twig' %}

{% block title %}CategorieEvenement Details{% endblock %}

{% block body %}
  <div class="row mt-3">
                  <div class="col-md-12">
    <a href="{{ path('app_categorie_evenement_index') }}" class="btn btn-secondary">
        <i class="fas fa-arrow-left"></i>
    </a>
</div>

    <div class="container mt-5">
        <h1 class="mb-4"><i class="fas fa-info-circle text-primary mr-2"></i>CategorieEvenement Details</h1>

        <div class="card shadow">
            <div class="card-body">
                <dl class="row">
                    <dt class="col-sm-3">ID</dt>
                    <dd class="col-sm-9">{{ categorie_evenement.id }}</dd>

                    <dt class="col-sm-3">NomCategorie</dt>
                    <dd class="col-sm-9">{{ categorie_evenement.nomCategorie }}</dd>

                    <dt class="col-sm-3">Description</dt>
                    <dd class="col-sm-9">{{ categorie_evenement.description }}</dd>
                </dl>
            </div>
        </div>
<div>
     
            <a href="{{ path('app_categorie_evenement_edit', {'id': categorie_evenement.id}) }}" class="btn btn-primary ml-2">
                <i class="fas fa-edit mr-2"></i>Edit
            </a>
        </div>

        {{ include('categorie_evenement/_delete_form.html.twig') }}
    </div>
{% endblock %}
