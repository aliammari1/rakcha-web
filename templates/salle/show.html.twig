{% extends 'base.html.twig' %}

{% block title %}Salle{% endblock %}

{% block body %}
    <h1>Salle</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdSalle</th>
                <td>{{ salle.idSalle }}</td>
            </tr>
            <tr>
                <th>NbPlaces</th>
                <td>{{ salle.nbPlaces }}</td>
            </tr>
            <tr>
                <th>NomSalle</th>
                <td>{{ salle.nomSalle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_salle_index') }}">back to list</a>

    <a href="{{ path('app_salle_edit', {'idSalle': salle.idSalle}) }}">edit</a>

    {{ include('salle/_delete_form.html.twig') }}
{% endblock %}
