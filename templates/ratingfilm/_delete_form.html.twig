<form method="post" action="{{ path('app_ratingfilm_delete', {'idFilm': ratingfilm.idFilm}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ratingfilm.idFilm) }}">
    <button class="btn">Delete</button>
</form>
