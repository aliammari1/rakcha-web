<form method="post" action="{{ path('app_series_delete', {'idserie': series.idserie}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ series.idserie) }}">
    <button class="btn">Delete</button>
</form>
