{% extends 'base.html.twig' %}

{% block title %}Series{% endblock %}

{% block body %}
    <h1>Series</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idserie</th>
                <td>{{ series.idserie }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ series.nom }}</td>
            </tr>
            <tr>
                <th>Resume</th>
                <td>{{ series.resume }}</td>
            </tr>
            <tr>
                <th>Directeur</th>
                <td>{{ series.directeur }}</td>
            </tr>
            <tr>
                <th>Pays</th>
                <td>{{ series.pays }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ series.image }}</td>
            </tr>
            <tr>
                <th>Liked</th>
                <td>{{ series.liked }}</td>
            </tr>
            <tr>
                <th>Nblikes</th>
                <td>{{ series.nblikes }}</td>
            </tr>
            <tr>
                <th>Disliked</th>
                <td>{{ series.disliked }}</td>
            </tr>
            <tr>
                <th>Nbdislikes</th>
                <td>{{ series.nbdislikes }}</td>
            </tr>
            <tr>
                <th>Idcategorie</th>
                <td>{{ series.idcategorie }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_series_index') }}">back to list</a>

    <a href="{{ path('app_series_edit', {'idserie': series.idserie}) }}">edit</a>

    {{ include('series/_delete_form.html.twig') }}
{% endblock %}
