{% extends 'base.html.twig' %}

{% block title %}CommentaireProduit{% endblock %}

{% block body %}
    <h1>CommentaireProduit</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commentaire_produit.id }}</td>
            </tr>
            <tr>
                <th>IdCommentaire</th>
                <td>{{ commentaire_produit.idCommentaire }}</td>
            </tr>
            <tr>
                <th>Commentaire</th>
                <td>{{ commentaire_produit.commentaire }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_commentaire_produit_index') }}">back to list</a>

    <a href="{{ path('app_commentaire_produit_edit', {'id': commentaire_produit.id}) }}">edit</a>

    {{ include('commentaire_produit/_delete_form.html.twig') }}
{% endblock %}
