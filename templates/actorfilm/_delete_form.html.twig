<form method="post" action="{{ path('app_actorfilm_delete', {'idactor': actorfilm.idactor}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ actorfilm.idactor) }}">
    <button class="btn">Delete</button>
</form>
