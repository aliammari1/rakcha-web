<form method="post" action="{{ path('app_filmcoment_delete', {'id': filmcoment.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ filmcoment.id) }}">
    <button class="btn">Delete</button>
</form>
