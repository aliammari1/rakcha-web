<form method="post" action="{{ path('app_cinema_delete', {'idCinema': cinema.idCinema}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cinema.idCinema) }}">
    <button class="btn">Delete</button>
</form>
