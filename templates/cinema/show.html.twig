{% extends 'base.html.twig' %}

{% block title %}Cinema{% endblock %}

{% block body %}
    <h1>Cinema</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdCinema</th>
                <td>{{ cinema.idCinema }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ cinema.nom }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ cinema.adresse }}</td>
            </tr>
            <tr>
                <th>Responsable</th>
                <td>{{ cinema.responsable }}</td>
            </tr>
            <tr>
                <th>Logo</th>
                <td>{{ cinema.logo }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{{ cinema.statut }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_cinema_index') }}">back to list</a>

    <a href="{{ path('app_cinema_edit', {'idCinema': cinema.idCinema}) }}">edit</a>

    {{ include('cinema/_delete_form.html.twig') }}
{% endblock %}
