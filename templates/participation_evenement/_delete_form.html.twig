<form method="post" action="{{ path('app_participation_evenement_delete', {'idParticipation': participation_evenement.idParticipation}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ participation_evenement.idParticipation) }}">
    <button class="btn">Delete</button>
</form>
