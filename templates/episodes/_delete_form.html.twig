<form method="post" action="{{ path('app_episodes_delete', {'idepisode': episode.idepisode}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ episode.idepisode) }}">
    <button class="btn">Delete</button>
</form>
