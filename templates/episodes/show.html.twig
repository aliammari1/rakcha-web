{% extends 'base.html.twig' %}

{% block title %}Episodes{% endblock %}

{% block body %}
    <h1>Episodes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idepisode</th>
                <td>{{ episode.idepisode }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ episode.titre }}</td>
            </tr>
            <tr>
                <th>Numeroepisode</th>
                <td>{{ episode.numeroepisode }}</td>
            </tr>
            <tr>
                <th>Saison</th>
                <td>{{ episode.saison }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ episode.image }}</td>
            </tr>
            <tr>
                <th>Video</th>
                <td>{{ episode.video }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_episodes_index') }}">back to list</a>

    <a href="{{ path('app_episodes_edit', {'idepisode': episode.idepisode}) }}">edit</a>

    {{ include('episodes/_delete_form.html.twig') }}
{% endblock %}
