<form method="post" action="{{ path('app_filmcategory_delete', {'filmId': filmcategory.filmId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ filmcategory.filmId) }}">
    <button class="btn">Delete</button>
</form>
